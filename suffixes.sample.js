/**
 * Configured suffixes file sample.
 * 
 * Create a suffixes.local.js file following this template and with your own entries.
 * 
 * You can use the following placeholders during the responses:
 * - <MATCH_UPPER>: the word matched, capitalized.
 * - <MATCH>: the word matched, as-is.
 */
module.exports = {
    'ino': [
        '¿<MATCH_UPPER>? Pues para <MATCH>... ¡Agárrame el pepino!'
    ]
}